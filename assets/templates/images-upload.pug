input(type="file" nv-file-select uploader="fileUploader" multiple)
button(ng-click="fileUploader.uploadAll();") Upload All
ul
    li(ng-repeat="item in fileUploader.queue") Name: {{item.file.name}}
        div(ng-thumb='{ file: item._file, height: 200 }')
        button(ng-click="item.upload();") Upload