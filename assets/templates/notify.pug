div(class="notifier" class = "{{notifier.state}}" ng-class="{visible : notifier.active}")
    div.notifier__message
        i(ng-click="notifier.close();" class="notifier__close fa fa-times" aria-hidden="true")
        span(ng-bind-html="notifier.message")