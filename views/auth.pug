extends layout

block header
    include partials/_navbar

block content
    h1= type
    .auth(ng-controller="authCtrl as auth")
        if type == "login"
            include partials/_login
        else if type == "registration"
            include partials/_registration

block footer
    include partials/_footer