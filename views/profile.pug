extends layout

block header
    include partials/_navbar

block content
    h1 PROFILE

    div(advertiser=currentUser._id edit="true")

    include partials/_new-advert-form
    include partials/_remove-advertiser

    if currentUser
        div(adverts-feed=currentUser._id)

block footer
    include partials/_footer