extends layout

block header
    include partials/_header

block content
    h1 PROFILE

    div(author=currentUser._id edit="true" remove="true")

    div(popup type="New Advert")
        popup-trigger
            button New Advert
        popup-content
            .popup__content
                advert-create(popup-close="$parent.popup.close();")

    feed(user-id=currentUser._id hide-fields="{author:true, publicationDate:true}")

block footer
    include partials/_footer