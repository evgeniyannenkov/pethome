div(popup type="Remove Account")
    popup-trigger
        button Remove
    popup-content(ng-controller="advertiserCtrl as remover")
        form.popup__content
            button(type="button" ng-click="remover.remove('" + currentUser._id + "');") Remove
            button(type="button" ng-click="remover.cancel();") Cancel